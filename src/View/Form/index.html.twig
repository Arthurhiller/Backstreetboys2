{% extends 'layout.html.twig' %}

{% block content %}
    {% include "_formRegistration.html.twig" %}

    <h1>Users</h1>
    <ul>
    {% for user in users %}
        <li><a href="/Form/show/{{ item.id }}">{{ item.id }}. {{ item.username }}</a></li>
    {% else %}
        <li>Nothing to display</li>
    {% endfor %}

{% endblock %}